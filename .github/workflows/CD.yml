name: 🏗 Build

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
  release:
    types: [published, edited, prereleased]
  merge_group:
  workflow_dispatch:

jobs:
  build:
    name: 🔨 Build and push docker image
    uses: WGBH-MLA/.github/.github/workflows/build.yml@v1